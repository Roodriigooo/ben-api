{
  "name": "ben-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  }
}

